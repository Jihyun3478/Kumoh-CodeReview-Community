spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kcr?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: hoon@0815!!

  jpa:
    hibernate:
      # 개발 초기 단계 또는 로컬에서 테스트: create 또는 update
      # 테스트 서버: update 또는 validate
      # 스테이징 및 운영 서버: validate 또는 none
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    generate-ddl: true

    properties:
      hibernate:
        format_sql: true
  cache:
    type: redis

  redis:
    host: localhost
    port: 6379

  jwt:
    secret: kcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcrkcr

  mail:
    host: smtp.gmail.com
    port: 587
    username: kumoh.kcr@gmail.com
    password: ncogpnozcbjiluze
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging.level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE